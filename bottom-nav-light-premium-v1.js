/* DCC legacy compatibility stub.
   The trainer bottom navigation is owned exclusively by coach-premium-core-v13.js.
   This file intentionally performs no styling, theme mutation, DOM mutation or event handling.
   Kept temporarily because an older loader can still request this path. */
(function(){
  'use strict';
  window.__dccBottomNavLightPremium='retired';
})();
