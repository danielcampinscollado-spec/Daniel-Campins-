name: Archived legacy client plan integrator
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived during full-app stabilization. Plan logic changes require reviewed source commits."
