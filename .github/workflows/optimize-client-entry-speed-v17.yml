name: Archived legacy client entry optimizer
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived during full-app stabilization. Client loading is audited in source, not rewritten by CI."
