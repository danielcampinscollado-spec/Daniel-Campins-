name: Archived legacy source rewrite workflow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived during full-app stabilization. CI must validate source, not rewrite it."
