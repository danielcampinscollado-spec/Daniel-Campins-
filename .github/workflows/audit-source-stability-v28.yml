name: Archived mutating source stability workflow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived during full-app stabilization. Stability checks are read-only and source fixes are committed explicitly."
