name: Archived legacy nutrition installer
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived during full-app stabilization. Source changes are managed directly and reviewed before promotion."
